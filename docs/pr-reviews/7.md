# PR Review Log #7

**PR**: [feat: add idea-browser skill — demand intelligence engine (REX-1041) #7](https://github.com/rexterity/hermes-agent/pull/7)
**Reviewed by**: Devin (REX-1044)
**Date**: 2026-03-21

## Summary
- **Scope**: New `idea-browser` skill under `skills/research/` — demand intelligence engine aggregating Google Trends, TikTok, Reddit, and Polymarket into a composite 0-100 score. Two files: `SKILL.md` (183 lines) + `idea_browser.py` (907 lines).
- **Risk**: Medium — touches external APIs without authentication; no tests; untested against live endpoints.
- **Positives**:
  - Clean module architecture with well-defined sections (Google Trends, TikTok, Reddit, Polymarket, Scorer)
  - Zero external dependencies — stdlib only, consistent with existing skills (`domain-intel`, `polymarket`)
  - Comprehensive SKILL.md following the established format with frontmatter, trigger conditions, command tables, and usage examples
  - Good error handling in `_get` (catches HTTPError/URLError gracefully)
  - Parallel execution via `ThreadPoolExecutor` in scorers and `full_scan`
  - Sensible deduplication in `reddit_pain_points`
- **Concerns** (see inline comments on PR):
  1. `full_scan` duplicates all API calls — fetches platform data, then `demand_score()` re-fetches everything (doubles requests + rate-limit exposure)
  2. `_get_json` lacks `JSONDecodeError` handling — non-JSON 200 responses cause unhandled crash in CLI commands
  3. Concurrent Reddit requests in `full_scan` risk 429 throttling (unauthenticated API ~10 req/min)
  4. `_score_tiktok` bidirectional substring match is overly permissive for short hashtags
  5. `int()` on CLI flag values crashes on non-integer input without a helpful message

## Decision
**Request changes.**

The core architecture and module design are solid. The three major issues (duplicated API calls in `full_scan`, missing JSONDecodeError handling, Reddit rate-limit risk) should be addressed before merge — they affect reliability in production use, which is the explicit purpose of the REX-1044 end-to-end testing ticket.

## Next steps
1. Refactor `full_scan` to pass pre-fetched data to scoring functions (or compute scores from existing results) to eliminate duplicated API calls
2. Add `try/except json.JSONDecodeError` in `_get_json` to return an error dict on non-JSON responses
3. Serialize or rate-limit Reddit requests in `full_scan` to stay within unauthenticated API limits
4. Add minimum-length guard to TikTok substring matching (line 659)
5. Wrap `int()` flag parsing with error handling for CLI robustness
